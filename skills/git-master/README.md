# Git Master

Routes git operations to the `git-master` agent: atomic commits, interactive rebasing, branch management, and history cleanup that matches the repo's existing commit style.

## When to Use

- Splitting a messy diff into atomic, conventional commits
- Rebasing or cleaning up branch history
- Any git operation where commit-message style should match repo history

---
